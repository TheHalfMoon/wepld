# Build Learning Ledger

| ID | Tool/source | Stable evidence reference | Context | Class | Observation | Status |
|---|---|---|---|---|---|---|
| BL-0001 | Continue | `continuedev/continue@5522c6f44ca0ac3528b37244818fbfa39b5af470` + `docs/acquisition/SOURCE_REGISTRY_INDEX.md` | build/source review | POSITIVE_MECHANISM | Source-controlled rules/checks and explicit planning/agent modes are a candidate behavior oracle; exact path-level corroboration remains pending. | UNVERIFIED |
| BL-0002 | Continue | `continuedev/continue@5522c6f44ca0ac3528b37244818fbfa39b5af470` + `docs/acquisition/SOURCE_REGISTRY_INDEX.md` | authority review | NEGATIVE_ORACLE | Agent/tool automatic modes must never be interpreted as WePLD effect authority; exact path-level corroboration remains pending. | UNVERIFIED |
| BL-0003 | Continue | `continuedev/continue@5522c6f44ca0ac3528b37244818fbfa39b5af470` README at pinned revision + canonical source registry artifact | source acquisition | MAINTENANCE_EVIDENCE | The pinned upstream repository identifies the project as no longer actively maintained/final-release; mine code/tests but avoid a non-replaceable foundation dependency without an explicit maintenance/exit decision. | OBSERVED |
| BL-0004 | WePLD trusted-base admission | `.github/workflows/s1-admission-integrity.yml@9a826e14fa2dd213f656b0ea2fec1ff737eb56dd` + PR #6 activation canary + run `33082784611` / #581 | S1 admission/control plane | POSITIVE_MECHANISM | Privileged `pull_request_target` verification can keep the trusted policy on the base, avoid candidate checkout/execution, and inspect candidate Git tree/blob objects strictly as data while still enforcing exact-delta admission. | QUALIFIED |
| BL-0005 | WePLD append-only successor policy | v17 blob `a40f377a08b47a5270194c9d1f77961772a448ac` + PR #198 merge `ff03b1ae4fcf8ee81fbacfa01102e94b1fa24dc2` + Foundation #742 + PR #199 merge `9a826e14fa2dd213f656b0ea2fec1ff737eb56dd` + Foundation #744 | authority lifecycle | POSITIVE_MECHANISM | Content-addressed successor wrappers, exact authorized path sets, predecessor projection, guarded merge, and post-merge activation prevent a candidate from self-expanding authority while still allowing bounded canonical state transitions. | QUALIFIED |
| BL-0006 | Qodo + S1 performance repair | `s1-014-review-evidence.md` blob `4fd85d2a1f829a63b840020c0e434aeb12f20328` + PR #197 + repaired workflow blob `3ccd118aea80fd31866973371babc329913aafb8` + performance run `33069506387` / #8 | benchmark evidence freshness | NEGATIVE_ORACLE | A benchmark workflow whose path filters omit any measured build input can leave green historical measurements stale; trigger coverage must include every input that can change the measured artifact. | QUALIFIED |
| BL-0007 | WePLD Security Review Policy | `docs/canonical/SECURITY_REVIEW_POLICY.md` blob `d6c0e85f7d4395ac19023d4c6e215cbe69821f9c` + `s1-014-review-evidence.md` + `s1-015-repair-evidence.md` blob `384b99d46eef802ef04cb3674bce3fee8a64be83` | security coverage accounting | NEGATIVE_ORACLE | An unavailable specialist security surface must remain an explicit non-PASS coverage status; correctness-review success cannot be relabeled as a security PASS or erase the coverage limitation. | QUALIFIED |
| BL-0008 | External review egress reconciliation | `docs/canonical/EXTERNAL_REVIEW_EGRESS_POLICY.md` blob `63828b821775d2a3659e9b7478bd78d96c0cdb98` + PR #197 preflight comment `5438429825` + PR #199 reconciliation comments `5440729035` and `5440732757` | hosted-review egress | NEGATIVE_ORACLE | A pre-egress approval must be recorded before transmission and its durable record must satisfy the required schema; later record completion may repair traceability but must never be represented as retroactive pre-review authorization. | QUALIFIED |
| BL-0009 | S1-014 topology finding normalization | `s1-014-review-evidence.md` blob `4fd85d2a1f829a63b840020c0e434aeb12f20328` + trusted-base admission runs #561 and #562 | review topology / false-positive handling | NEGATIVE_ORACLE | A synthetic review base can surface topology failures that do not exist on the canonical trusted-base route; findings must be reproduced against the authoritative execution topology before they are treated as material defects. | QUALIFIED |
| BL-0010 | CodeRabbit + WePLD S2 identity/store tranche | PR #240 round-7 review comment `5479628135` + repaired head `15bf94eb4cccd395e629e2e129605f5af8ce2a6d` | test-evidence integrity | NEGATIVE_ORACLE | A test whose only active assertion is one the implementation already guarantees internally cannot fail for the property its name claims. Two instances were found in one tranche: a helper documented as a compile-failure probe for a negative auto-trait bound whose only live assertion was the positive case, and a reader/writer race test whose sole assertion was an equality the read API refuses to violate before returning. Audit what a test would have to observe to fail, not what it is named. | QUALIFIED |
| BL-0011 | WePLD local gate runner | PR #240 round-9 disposition comment `5480043818` + repaired head `2cc4098fb2351a133aa434e8a263ac6e45bbc11f` | qualification harness | NEGATIVE_ORACLE | A gate script using `set -e` inside a subshell whose output is piped can continue past a failing gate, so a run can be reported as a pass while a gate actually failed. It was caught only because the failure printed a visible diff. Check each gate's exit code individually; a gate runner that cannot fail is the same defect class as a test that cannot fail. | QUALIFIED |
| BL-0012 | CodeRabbit finding reproduction under frozen policy | PR #240 round-8 review comment `5479782067` + regression `a_different_store_root_cannot_launder_a_held_project_guard` + repaired head `b295a89a9306dd88b921e3ab1a938bdea348f770` | finding reconciliation | POSITIVE_MECHANISM | When a reviewer's suggested fixture is prohibited by frozen policy, reproducing the finding through an equivalent permitted fixture preserves fail-before proof instead of downgrading the repair to reasoning. A symlink alias could not be created because `symlink` is a forbidden product token; two lexically distinct store roots exhibited the identical evasion and failed against the defective implementation. Record which suggestion was declined and why, so a declined suggestion is not mistaken for an unaddressed finding. | QUALIFIED |
| BL-0013 | WePLD S2 evidence-store bound statements | PR #240 round-15 comment `5482772392` and round-16 comment `5482839026` + heads `0b65ea4626602488b923c098807cdda84d109d0e` and `bdebfbaa8f146115321e6d204da9e49d367047e2` | claim-over-evidence | NEGATIVE_ORACLE | A stated bound is a claim under review in both directions. One round denied an aggregate read ceiling that in fact existed and derived from frozen cardinality and per-artifact constants; the next round found the corrected ceiling still inexact because the bounded read requested a whole buffer past the limit before refusing. Understating a guarantee hides real worst-case cost from qualification exactly as effectively as overstating one, and a worst case that holds only for well-formed input is not a worst case. | QUALIFIED |
| BL-0014 | WePLD S2 contract identifiers vs path projection | PR #240 round-12 disposition comment `5482595224` + round-13 head `42789c6554a22a899292f0e3f51ea6810a26b2a5` + regressions for reservation, orphan and `CURRENT` binding | persisted-artifact binding | NEGATIVE_ORACLE | When a contract identifier charset is wider than the filesystem path projection that stores it, an artifact can agree with its own filename and still be one the writer could never have produced or addressed. Binding a persisted record to an identifier parsed from its path is insufficient; bind it to the exact path the writer would produce. The same gap appeared independently in reservation reads, orphan enumeration and the current-generation pointer. | QUALIFIED |
| BL-0015 | WePLD S2 store defect classification | PR #240 round-14 comment `5482707656` + repaired head `0f3e773b7187e156d9fe2aaed65ded8d43333586` | recovery semantics | NEGATIVE_ORACLE | Flattening every persisted-read defect into "nothing is published" made a corrupt pointer report a live, validly published generation as orphan residue. Undetermined is not absent. Only the absence of the pointer establishes that nothing is published; every other defect means the current selection cannot be determined and must propagate, because a residue listing is the input a later recovery flow may act on destructively. | QUALIFIED |
