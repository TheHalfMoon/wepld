# Git Branching Workflow Extension Configuration
# Copied to .agile/extensions/git/git-config.yml on install

# Branch numbering strategy: "sequential" (001, 002, ...) or "timestamp" (YYYYMMDD-HHMMSS)
branch_numbering: sequential

# Optional branch name template. Leave empty for the default "{number}-{slug}".
# Supported tokens: {author}, {app}, {number}, {slug}
# {slug} must not appear before {number}; final path segment must start with {number}-.
# Example for monorepos: "{author}/{app}/{number}-{slug}"
branch_template: ""

# Optional shorthand namespace. Leave empty to use branch_template/default behavior.
# Example: "features/{app}" expands to "features/{app}/{number}-{slug}"
branch_prefix: ""

# Commit message used by `git commit` during repository initialization
init_commit_message: "[Agile] Initial commit"

# Commit message style used by auto-commit hooks (agile.git.commit):
# "fixed"        - default; use the configured/static messages below.
# "conventional" - ask the agent to inspect the diff and generate a
#                  Conventional Commit message (e.g. "feat: add OAuth spec")
#                  instead of using the messages configured below.
commit_style: fixed

# Auto-commit before/after core commands.
# Set "default" to enable for all commands, then override per-command.
# Each key can be true/false. Message is customizable per-command.
auto_commit:
  default: false
  before_clarify:
    enabled: false
    message: "[Agile] Save progress before clarification"
  before_plan:
    enabled: false
    message: "[Agile] Save progress before planning"
  before_tasks:
    enabled: false
    message: "[Agile] Save progress before task generation"
  before_implement:
    enabled: false
    message: "[Agile] Save progress before implementation"
  before_checklist:
    enabled: false
    message: "[Agile] Save progress before checklist"
  before_analyze:
    enabled: false
    message: "[Agile] Save progress before analysis"
  before_taskstoissues:
    enabled: false
    message: "[Agile] Save progress before issue sync"
  after_constitution:
    enabled: false
    message: "[Agile] Add project constitution"
  after_specify:
    enabled: false
    message: "[Agile] Add specification"
  after_clarify:
    enabled: false
    message: "[Agile] Clarify specification"
  after_plan:
    enabled: false
    message: "[Agile] Add implementation plan"
  after_tasks:
    enabled: false
    message: "[Agile] Add tasks"
  after_implement:
    enabled: false
    message: "[Agile] Implementation progress"
  after_checklist:
    enabled: false
    message: "[Agile] Add checklist"
  after_analyze:
    enabled: false
    message: "[Agile] Add analysis report"
  after_taskstoissues:
    enabled: false
    message: "[Agile] Sync tasks to issues"
