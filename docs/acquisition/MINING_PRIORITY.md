# Source Mining Priority

This does not change S1-S10. It determines which donors to mine when a ratified capability reaches its acquisition gate.

- S1/S3: Codex + windows-rs + portable-pty + Tauri; DeepCode Windows Job Object/process-lifetime behavior as a path-level positive/negative oracle only.
- S4: Glean + Tree-sitter + SCIP + Zoekt + Tantivy + Augment/Greptile/Cohere behavior; DeepCode durable context/Skill/session mechanics; DeerFlow structured memory/durable context; LLM Space local Thread/run-history storage where directly relevant.
- S5: GitHub Spec Kit + deterministic schema/spec tooling + Fern; Kiro/Devin/CommandCode behavior; Agentica compiler/schema-validation mechanics; DeepCode Paper2Code planning/reference-mining behavior only when research-reproduction capability is in scope.
- S6: DeepSeek Harness + Cordis + Goose + Kilo + Eigent + exact CAMEL lineage + DeepCode bounded subagent/UWC mechanics + DeerFlow minimum-useful-subagent/deferred-tool mechanics + Agentica multi-protocol function normalization and selector strategy.
- S7: deterministic scanners + OpenCodeReview + Qodo + CodeRabbit + Cubic + Greptile + Augment + Graphite + Devin Review + DeepCode structured reviewer/evidence mechanics + LLM Space run comparison/rubric snapshot/trace-debug mechanics + Agentica deterministic validation failure corpus.
- S8: DeepCode interruption/resume/no-side-effect-replay and recovery behavior + DeerFlow capped-result/durable-event/recovery mechanics + LLM Space source-preserving compaction/replay behavior as candidates/test quarries, alongside separately qualified recovery sources selected for the exact task.
- S9/S10: JJ + gh-stack + Git Town + CommandCode Taste + Devin session learning; Greptile/DeepCode/DeerFlow/LLM Space feedback and run evidence may be mined only as non-authoritative Byan learning candidates.

Ponytail rule: mine 2–3 strongest candidates for the exact capability, retain only the minimum machinery that improves accepted outcomes, and preserve an exit path.

Registry-state rule:

```text
DeerFlow = existing frozen-registry source; enrichment only
DeepCode = post-registry-V1 candidate; NOT_ADMITTED
Agentica = post-registry-V1 candidate; NOT_ADMITTED
LLM Space = post-registry-V1 candidate; NOT_ADMITTED
```

These priority references do not change the 402-entry frozen restoration artifact, roadmap, or authorize source/dependency reuse.
